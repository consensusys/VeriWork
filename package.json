{
  "name": "veriwork",
  "version": "2.0.0",
  "description": "VeriWork: A Layer 2 Proof-of-Adaptive-Work platform for Web4 dApps with zero-knowledge verification",
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/andrewdong14/veriwork" },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node",
    "test:py": "python3 -m pytest tests -q",
    "sim": "python3 -m flexfactory.run_simulation --minutes 180 --accel 50"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  }
}
